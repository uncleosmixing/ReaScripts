# Project Rules for ReaTitles

- **Always push updates to GitHub**: Whenever code edits or additions are completed, stage all changes, commit them with a descriptive message, and push to the remote repository on GitHub (`git push origin main`).
